# RetailFlow Airflow (Optional)

- Use `retailflow_medallion_dag` to trigger the main Databricks job on a schedule.
- Configure `databricks_default` connection with host and token.
- Alternatively run Databricks Jobs natively without Airflow.
